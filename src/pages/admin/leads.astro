---
import AdminLayout from '../../layouts/AdminLayout.astro';
import AdminLeadsList from '../../components/admin/AdminLeadsList';
import { Users, LayoutDashboard, FileText } from 'lucide-react';
---

<AdminLayout title="Gelen Başvurular">
  <div class="min-h-screen bg-slate-50 flex">
    {/* Sidebar */}
    <aside class="w-64 bg-slate-900 text-white fixed h-full top-0 left-0">
      <div class="p-6">
        <h1 class="text-2xl font-bold font-display">İzmir<span class="text-secondary">Clinic</span></h1>
        <p class="text-xs text-slate-400 mt-1 uppercase tracking-wider">Admin Paneli</p>
      </div>
      
      <nav class="mt-6">
        <a href="/admin" class="flex items-center gap-3 px-6 py-4 text-slate-300 hover:bg-white/5 hover:text-white transition-colors">
          <LayoutDashboard className="w-5 h-5" />
          Genel Bakış
        </a>
        <a href="/admin/leads" class="flex items-center gap-3 px-6 py-4 text-white bg-white/10 transition-colors">
          <Users className="w-5 h-5" />
          Başvurular
        </a>
        <a href="/admin/content" class="flex items-center gap-3 px-6 py-4 text-slate-300 hover:bg-white/5 hover:text-white transition-colors">
          <FileText className="w-5 h-5" />
          İçerik Yönetimi
        </a>
      </nav>
    </aside>

    {/* Main Content */}
    <main class="flex-1 ml-64 p-8">
      <AdminLeadsList client:load />
    </main>
  </div>
</AdminLayout>
